server {
    listen 80;
    listen [::]:80;
    server_name __PUBLIC_APP_DOMAIN__;

    server_tokens off;
    access_log off;
    error_log /var/log/nginx/controle-gastos-error.log error;

    location / {
        return 503;
    }
}
